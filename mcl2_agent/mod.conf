# mcl2_agent — VLA 数据采集与 Agent 框架模组（骨架）
# 设计见仓库根目录 DESIGN.md §10
name = mcl2_agent
description = VLA data collection and agent framework for Mineclonia (MineClone2 lineage)
author = mcl2-env
version = 0.1.0
license = MIT

# 依赖 Mineclonia（或 VoxeLibre）游戏提供的 API；无硬性 mod 依赖
depends =
optional_depends =
